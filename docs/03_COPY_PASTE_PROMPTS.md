# FO’X — Copy/Paste Prompts

## Старт нового чата в проекте

```text
Работаем внутри проекта FO’X. Используй проектные инструкции. Сначала коротко проверь текущий контекст: Mini App, банкеты, Cloudinary, Google Sheets, сток бара. Не переписывай рабочие файлы вслепую. Перед любыми правками скажи, какой файл меняешь и почему.
```

## Сделать новую функцию в Mini App

```text
Нужно добавить функцию в Telegram Mini App FO’X. 
Текущая рабочая версия: v12 Cloudinary READY.
Не ломай банкеты, загрузку фото Cloudinary и чек-листы.
Сначала напиши план: какие файлы меняются, какие части кода затрагиваются, какие риски.
Потом дай готовый код или файл.
В конце дай чеклист тестирования.
Функция: [ОПИШИ ФУНКЦИЮ]
```

## Исправить баг в Mini App

```text
В FO’X Mini App баг: [ОПИШИ БАГ].
Сначала не переписывай всё. Дай диагностику по шагам: frontend, Apps Script, Google Sheets, Cloudinary, Telegram cache.
После диагностики предложи минимальную правку.
Текущая рабочая версия: v12 Cloudinary READY.
```

## Обновить только внешний вид

```text
Нужно изменить только внешний вид FO’X Mini App.
Backend, Apps Script, Google Sheets и Cloudinary не трогай.
Сохрани все функции.
Измени только CSS/HTML в `index.html`.
Что изменить: [ОПИСАНИЕ]
```

## Обновить Apps Script backend

```text
Нужно изменить только Apps Script backend банкетов.
`index.html` не трогай.
Не запускай и не советуй запускать `setupBanquetsBackend()`, если это не нужно.
Сохрани совместимость с текущим `index.html`.
Что изменить: [ОПИСАНИЕ]
```

## Проверить проблему с фото

```text
В FO’X банкетах проблема с фото: [ОПИШИ].
Текущая схема: Mini App грузит фото в Cloudinary, потом Apps Script сохраняет ссылку в Google Sheets.
Проверь именно эту цепочку:
1. выбран ли файл;
2. ушёл ли upload в Cloudinary;
3. вернулся ли `secure_url`;
4. записался ли `Image URL` в Google Sheets;
5. отдаёт ли `action=list` ссылку;
6. показывает ли приложение фото.
```

## Работа со стоком

```text
Работаем с таблицей стока FO’X.
Главное правило: не потерять значения, формулы и стили.
Перед правками опиши, какие листы и колонки меняешь.
Структура стоковых листов:
A Наименование
B ЗАКАЗ
C Сток
D Остаток Аллея
E Остаток Заготовочный
F Цена
G Сумма остатка
H Ед. изм.
I Примечание

Формулы:
G = D + E
B = MAX(0, C - G)

Задача: [ОПИШИ ЗАДАЧУ]
```

## Исправить таблицу после поломки

```text
Таблица FO’X сток сломалась после правок.
Не предлагай хаотично менять всё.
Сначала найди:
1. какие листы пострадали;
2. какие формулы исчезли;
3. где значения стали пустыми;
4. где поплыл стиль;
5. какие листы отличаются от эталонной структуры.
Потом предложи восстановление в новой копии файла, не поверх старой.
```

## Сделать релизную заметку

```text
Составь короткий changelog для FO’X версии [НОМЕР].
Формат:
- что изменено;
- какие файлы затронуты;
- что проверить;
- риски;
- как откатиться.
```

## Перед публикацией новой версии

```text
Проверь перед релизом FO’X:
1. не удалены ли рабочие константы;
2. не стоят ли заглушки `PASTE...`;
3. не сломан ли `BANQ_API_URL`;
4. не изменены ли admin IDs;
5. не тронут ли backend без необходимости;
6. есть ли новый номер `?v=...`;
7. есть ли чеклист тестов.
```

## Попросить полный файл

```text
Мне нужен не кусок, а полный готовый файл.
Дай весь файл целиком и назови его точным именем.
Сначала укажи, что именно заменить.
Не пропускай старые рабочие функции.
```

## Попросить инструкцию для телефона

```text
Я делаю с телефона. Дай инструкцию максимально просто:
1. что открыть;
2. куда нажать;
3. что скопировать;
4. куда вставить;
5. как проверить.
Без длинной теории.
```
