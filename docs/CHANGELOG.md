# FO’X — Changelog

## Tatooine frontend v1.3.3 — HIDE EMPTY PREPAYMENTS

Статус: production с 22 июля 2026; PR №11, merge commit `cab4dfb72a3534276bc207b3eadc95bebb289344`, GitHub Pages опубликован, 35/35 автоматических тестов, живой Telegram-тест ожидается.

Что изменено:

- весь блок `Предоплаты`, включая заголовок и строку `Итого`, скрывается, если пользователь не добавил ни одной предоплаты;
- при наличии хотя бы одной предоплаты сохраняются прежние строки, интервалы и итоговая сумма.

Как откатиться:

- вернуть frontend Tatooine на v1.3.2 из merge commit `2851cd20ef9171f81481c491b8604d9cf25f9525`.

## Tatooine frontend v1.3.2 — DEFAULT CHANGE FUND 100K

Статус: production с 22 июля 2026; PR №10, merge commit `2851cd20ef9171f81481c491b8604d9cf25f9525`, GitHub Pages опубликован, 35/35 автоматических тестов, живой Telegram-тест ожидается.

Что изменено:

- стандартное значение поля `Неизменный размен` для Tatooine изменено с `70 000` на `100 000`;
- значение применяется при открытии приложения и после распознавания нового отчёта;
- поле остаётся редактируемым вручную.
- строка `Итого` в блоке предоплат выводится только при наличии добавленных предоплат.

Как откатиться:

- вернуть frontend Tatooine на v1.3.1 из merge commit `3e2aed91a9b00e691f70918eba3674be43061535`.

## Tatooine frontend v1.3.1 — PAYMENT ROW PREFIX FIX

Статус: production с 22 июля 2026; PR №9, merge commit `3e2aed91a9b00e691f70918eba3674be43061535`, GitHub Pages опубликован, 34/34 автоматических теста, повторный живой OCR-тест ожидается.

Что исправлено:

- строки `Оплата EatAndSplit` и `Оплата EatAndSplit Продажа` распознаются как `EatAndSplit`;
- тот же префикс `Оплата` и служебные окончания поддержаны для Яндекс еды;
- остальные строки платежей и backend не изменялись.

Как откатиться:

- вернуть frontend Tatooine на v1.3.0 из merge commit `e84133c1688b067adc1df0c141e2694ade7ccb37`.

## Stock/scanner backend v9.4.9 — ENVELOPE COLLECTION SEPARATORS

Статус: production с 22 июля 2026; Apps Script version `21`, публичный `ping v9.4.9`, 34/34 автоматических теста и контрольный тест синтетических фото, живой OCR-тест конверта ожидается.

Что изменено:

- инкассация с конверта распознаётся не только в скобках, но и после разделителей `/`, `\\` и `-`;
- добавлен реальный пример `Инкассация: 90616 / 96620` → `90 616 (96 620)`;
- источник инкассации не изменён: только рукописная надпись на конверте, без вычислений из iiko.

Как откатиться:

- переключить Apps Script deployment обратно на version `20` (`v9.4.8`).

## Tatooine frontend v1.3.0 — IIKO 041 REPORT TEMPLATE

Статус: production с 22 июля 2026; PR №8, merge commit `e84133c1688b067adc1df0c141e2694ade7ccb37`, GitHub Pages опубликован, 33/33 автоматических теста, живой Telegram-тест ожидается.

Что изменено:

- в форме и итоговом сообщении оставлены только строки из утверждённого образца;
- удалены `Безнал 2`, `Нал 2`, Tapper, оба расчётных счёта и `На утро в кассе`;
- фискальная и нефискальная наличность объединяются в одно редактируемое поле `Нал`;
- все оставшиеся строки по-прежнему показываются даже при пустом значении.

Как откатиться:

- вернуть frontend Tatooine на v1.2.1 из merge commit `524fbb2e09651919ed3bb86d4ce8bec495575ec9`.

## Stock/scanner backend v9.4.8 — CASH IIKO 041 SOURCE

Статус: production с 22 июля 2026; Apps Script version `20`, публичный `ping v9.4.8`, 33/33 автоматических теста, живой OCR-тест отчёта 041 ожидается.

Что изменено:

- маршруты FO’X и Tatooine используют отчёт iiko 041 как единственный источник даты, общей выручки и платёжных строк;
- значения других отчётов iiko не подставляются вместо неподтверждённого отчёта 041;
- терминальные слипы и конверт продолжают распознаваться по прежним правилам;
- Telegram-маршрутизация и отдельные шаблоны сообщений ресторанов сохранены.

Как откатиться:

- переключить Apps Script deployment с новой версии обратно на version `19` (`v9.4.7`).

## Tatooine frontend v1.2.1 — ALWAYS SHOW REPORT FIELDS

Статус: production с 22 июля 2026; PR №7, merge commit `524fbb2e09651919ed3bb86d4ce8bec495575ec9`, GitHub Pages опубликован, 31/31 автоматических тестов, живой Telegram-тест ожидается.

Что изменено:

- все денежные строки всегда остаются в финальном отчёте;
- при отсутствии значения после подписи остаётся пустое место вместо `0`;
- пустой блок предоплат сохраняет строку `Итого:` без суммы;
- введённый вручную ноль по-прежнему отображается как `0`.

Как откатиться:

- вернуть frontend Tatooine на v1.2.0 из merge commit `daed7bba8edffdef19eb525f01a7ce5eec115a6e`.

## Tatooine frontend v1.2.0 — ORIGINAL TELEGRAM LAYOUT

Статус: production с 22 июля 2026; PR №6, merge commit `daed7bba8edffdef19eb525f01a7ce5eec115a6e`, 31/31 тест, живой Telegram-тест ожидается.

Что изменено:

- восстановлены точные пустые строки и порядок блоков из оригинального сообщения;
- заменены fallback-эмодзи на оригинальные: `🪙`, `🧪`, `📈`, `🌎`, `💀`, `🔠`, `🔄`;
- `Нал` и `Нал Фискал` объединяются в одну итоговую строку `Нал`, исходные поля формы остаются раздельными;
- жирным оформляются только общая выручка, подписи расхода и инкассации;
- дата остаётся курсивом, строки платежей — обычным текстом.

Как откатиться:

- вернуть frontend Tatooine на v1.1.1 из merge commit `e1e184acb97b3e9a5dc76623521a90d3b8520285`.

## Stock/scanner backend v9.4.7 — TATOOINE ORIGINAL LAYOUT

Статус: production с 22 июля 2026; Apps Script version `19`, публичный `ping v9.4.7`, живой Telegram-тест ожидается.

Что изменено:

- HTML-разметка Tatooine повторяет жирность, курсив и интервалы оригинального сообщения;
- строки без пробела после custom emoji (`EatAndSplit`, `Яндекс еда`, `Предоплаты`) распознаются корректно;
- оформление FO’X не изменялось.

Как откатиться:

- переключить Apps Script deployment с version `19` на version `18` (`v9.4.6`).

## Tatooine frontend v1.1.1 — FILE INPUT FIX

Статус: production с 22 июля 2026; PR №5, merge commit `e1e184acb97b3e9a5dc76623521a90d3b8520285`, 31/31 тест, повторный Telegram-тест ожидается.

Что исправлено:

- выбранные файлы копируются в обычный массив до очистки `<input type="file">`;
- Telegram WebView больше не должен терять выбранные фотографии при закрытии камеры или галереи;
- обработка фотографий, OCR и backend не изменялись.

Как откатиться:

- вернуть frontend Tatooine на v1.1.0 из merge commit `c66fa5396504d9465bafc3cbb2770213c15e073c`.

## Tatooine frontend v1.1.0 — PETROVKA REPORT TEMPLATE

Статус: production с 22 июля 2026; PR №4, merge commit `c66fa5396504d9465bafc3cbb2770213c15e073c`, 30/30 тестов, живой Telegram-тест ожидается.

Что изменено:

- сообщение строится по предоставленному образцу Tatooine/Petrovka;
- добавлены редактируемые строки EatAndSplit и Яндекс еда из точных `paymentRows` OCR;
- нулевые дополнительные способы оплаты скрываются;
- добавлен итог предоплат и формат сумм с точками;
- обязательные строки `Расчётный счёт` и `Расчётный счёт 2` сохранены раздельно.

Как откатиться:

- вернуть frontend Tatooine на v1.0.1 из merge commit `4d5c254e56760715e78f01d08272d6ef5dde5cd7`.

## Stock/scanner backend v9.4.6 — TATOOINE REPORT TEMPLATE

Статус: production с 22 июля 2026; Apps Script version `18`, публичный `ping v9.4.6`, custom emoji 8/8 и 11/11 захвачены, живой Telegram-тест ожидается.

Что изменено:

- Telegram HTML-шаблон Tatooine отделён от оформления FO’X;
- заголовок Tatooine больше не подменяется логотипом FO’X;
- добавлен безопасный захват custom emoji из пересланного эталонного сообщения через `tatooineCashCaptureTelegramStyle()`;
- при отсутствии или отклонении custom emoji сохраняется отправка обычного текста.

Как откатиться:

- переключить Apps Script deployment с version `18` на version `17` (`v9.4.5`).

## Tatooine frontend v1.0.1 — SEPARATE BOT ROUTING

Статус: production с 22 июля 2026; PR №3, merge commit `4d5c254e56760715e78f01d08272d6ef5dde5cd7`, живой Telegram-тест ожидается.

Что изменено:

- frontend передаёт неизменяемый маршрут `venue=tatooine` во всех запросах;
- приложение готово к запуску только из отдельного Telegram-бота Tatooine;
- публичный Apps Script Web App URL сохранён.

Как откатиться:

- вернуть `frontend/tatooine/config.js` и `app.js` к v1.0.0.

## Stock/scanner backend v9.4.5 — MULTI BOT CASH ROUTING

Статус: production с 22 июля 2026; Apps Script version `17`, публичный `ping v9.4.5`, живой Telegram-тест ожидается.

Что изменено:

- добавлен отдельный Telegram-маршрут Tatooine на существующем Web App URL;
- токен, целевой чат и список пользователей Tatooine читаются только из отдельных Script Properties;
- маршруту Tatooine разрешены только действия кассового отчёта;
- задания кассового отчёта FO’X и Tatooine изолированы друг от друга;
- повторное использование чужого `jobId` не может перезаписать задание другого пользователя или бота;
- существующие свойства и маршрут бота FO’X сохранены.

Как откатиться:

- переключить рабочее Apps Script-развёртывание с version `17` на version `16` (`v9.4.4`).

## Tatooine frontend v1.0.0 — CASH REPORT ONLY

Статус: production с 22 июля 2026; GitHub Pages опубликован, 20/20 тестов и браузерный smoke-тест пройдены, Telegram-тест на реальных фото ожидается.

Что добавлено:

- отдельное приложение `Tatooine` только для кассового отчёта;
- модульная структура `index.html` + `styles.css` + `config.js` + `app.js`;
- использование существующего Apps Script обработчика FO’X v9.4.4;
- загрузка нескольких фото, изменение порядка, поворот и удаление;
- OCR-лимиты 20 страниц и 12 МБ до отправки;
- редактирование сумм, сверка терминалов, предоплаты и повторная отправка;
- сообщение Telegram с брендом `Tatooine`.

Как откатиться:

- отменить merge commit `4dd3219c6d4f470d9efb2d49f8e2c032d432b794` или отключить кнопку Mini App;
- Apps Script откатывать не требуется, потому что backend не изменяется.

## Frontend v15.13.3 — SCANNER JOB SAFETY

Статус: production с 22 июля 2026; GitHub Pages опубликован, браузерный smoke-тест пройден, Android/Telegram-тест ожидается.

Что изменено:

- старое фоновое создание PDF больше не может записать URL в результат нового сканирования;
- PDF строится по снимку страниц конкретного задания;
- отменённая PDF-загрузка прекращает polling и не меняет глобальное состояние;
- изменение, удаление и перестановка страниц документа или кассового отчёта отменяют результат предыдущего задания;
- до OCR проверяется максимум 20 страниц;
- общий размер OCR-изображений ограничен 12 МБ до отправки в Apps Script.

Файлы:

- `frontend/candidate/index.html`;
- `tests/frontend/scanner_state.test.js`;
- документация состояния проекта.

Что проверить вручную:

- сбросить сканер во время подготовки PDF и сразу распознать другой документ;
- документ из 1 и 3 страниц на Android;
- кассовый отчёт с длинным iiko, конвертом и двумя терминальными слипами;
- подтверждение стока и отправку документа без внесения позиции в сток.

Как откатиться:

- отменить GitHub merge commit `10f766c11deabfe0403af1640e266e6dbc998a6f`;
- дождаться повторной сборки GitHub Pages с предыдущим `index.html`.

## Stock/scanner backend v9.4.4 — CASH REPORT SOURCE RULES FIX

Статус: production с 22 июля 2026; Apps Script version `16`, публичный `ping` вернул `v9.4.4`, живой Telegram-сценарий ожидается.

Что изменено:

- итог терминального слипа берётся строго из блока «Количество оплат → На сумму»;
- сумма «Оплата + Оплата QR» используется только при отсутствии итогового блока;
- повторное OCR терминалов больше не выбирается по близости к сумме iiko;
- дата кассового отчёта берётся только из длинного отчёта iiko;
- добавлены автоматические тесты правил кассового отчёта.

Файлы:

- `apps-script/stock-scanner/candidate/Code.gs`;
- `tests/cash/cash_report_logic.test.js`;
- документация состояния проекта.

Что проверить вручную:

- отчёт с совпадающими iiko и терминалами;
- расхождение терминалов −1 000 ₽ не исчезает после повторного OCR;
- терминальный итог с QR берётся из нижнего итогового блока;
- дата терминала или конверта не заменяет дату iiko;
- исправленный отчёт повторно отправляется в Telegram.

Как откатиться:

- переключить рабочее Apps Script-развёртывание с version `16` обратно на version `15`;
- проверить `?action=ping`, он должен снова вернуть `v9.4.3 CASH REPORT VALIDATION FIX`.
