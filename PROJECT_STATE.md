# FO’X — состояние проекта

Актуально на 21 августа 2026 года.

## Архитектура

- FO’X: Telegram Mini App, опубликованная из корневого `index.html` через GitHub Pages.
- Tatooine: отдельная Mini App кассового отчёта в `tatooine/`, отдельный Telegram-бот.
- FO’X и Tatooine используют общий Apps Script обработчик сканера/кассового отчёта, но отдельные Telegram-маршруты.
- Банкетный модуль использует цепочку Mini App → Cloudinary → URL → Apps Script → Google Sheets.
- Gemini вызывается из Apps Script; ключи и токены должны находиться только в Script Properties.

Подробнее: `ARCHITECTURE.md` и документы в `docs/`.

## Подтверждённые версии и публикация

| Компонент | Версия | Статус | Основание |
| --- | --- | --- | --- |
| FO’X frontend | `v15.14.4` | опубликован в `main` | commit `c3c3b49` |
| Tatooine frontend | `v1.7.1 APP HUB` | опубликован в `main` и GitHub Pages | commit `c961773` |
| Scanner/cash Apps Script | `v9.6.2 TATOOINE ORIGINAL IMAGE OCR`, Apps Script version 29 | deployment обновлён и проверен `ping` | commit `227fe73`; URL deployment сохранён |
| Banquets Apps Script | неизвестно | требует подтверждения пользователя и проверки deployment | исходника production нет в `main` |
| Google Sheets | неизвестно | требует подтверждения пользователя | таблицы и Script Properties не хранятся в Git |

Frontend-релиз `v15.14.3` очищает хаб, таблицы и банкетный экран от декоративных повторяющихся пояснений, а кассовый отчёт — от технической подписи; рабочие инструкции, статусы и навигация сохранены. Предыдущий релиз `v15.14.2` убирает выбор «Документ / Ч/Б» из preview сканера и скрывает промежуточные описания при активном прогрессе; финальные сообщения и ошибки сохранены.

Tatooine-релиз `v1.7.1` выводит кассовый отчёт, такси и управление ролями в отдельные пункты хаба. Управление ролями показывается только `superadmin` и запрашивает список сотрудников по требованию; модуль такси ещё не подключён. RBAC-инфраструктура остаётся прежней: пользователь получает роль и permissions одним bootstrap-запросом, а первый `superadmin` задан через Script Property без его хранения в репозитории.

## Что есть только в ветках или candidate

- Полный candidate исходник scanner/cash Apps Script находится в ветке `codex/cash-slip-detail-montage`, но не в `main`.
- Расширенная внутренняя документация и тесты также находятся в рабочих ветках и не входят в публичный `main`.
- Фактическая production-версия banquet backend и схема его deployment неизвестны.

## Проверка

- Автоматические тесты RBAC-релиза: 36/36; lint и syntax-check прошли по commit `227fe73`.
- Публичные GitHub Pages версии FO’X `v15.14.4` и Tatooine `v1.7.1` доступны после push commits `c3c3b49` и `c961773`; HTML Tatooine проверен по публичному URL. Живой Telegram-тест остаётся отдельным пунктом.
- Публичный backend после обновления deployment version 29 ответил на `ping`.
- Живой пользовательский тест точности OCR и отправки в Telegram для текущего montage-релиза не подтверждён.
- Живой тест банкетов и формул Google Sheets после текущего frontend-релиза не подтверждён.

## Известные проблемы

- Терминальные чеки могли распознавать произвольные неверные цифры; пользователь подтвердил сильное ухудшение Tatooine на входном монтаже 2×2.
- Несколько банкетов на одну дату, снятие резерва после завершения и добавление новых позиций требуют проверки на фактическом production backend.
- Скорость сканера документов на реальном Android требует замера.
- Актуальные идентификаторы таблиц и значения Script Properties неизвестны репозиторию.

## Текущая задача

Провести живой тест Tatooine в Telegram под bootstrap-superadmin: открыть экран «Роли сотрудников», убедиться, что он отображается, а затем назначить постоянную роль через интерфейс. После появления второй роли `superadmin` bootstrap property можно удалить.

## Следующие шаги

1. Проверить точность цельного OCR-снимка `v1.5.2` внутри Telegram на Android на том же проблемном чеке.
2. Провести живой OCR/Telegram-тест FO’X и Tatooine на одинаковом наборе фото.
3. Зафиксировать результат пользовательского теста в этом файле.
4. Отдельно диагностировать банкетные проблемы по фактическому production backend без запуска setup-функций.
